pub struct AutoGenerated {
  age_of_the_universe : Array[AgeOfTheUniverse] // `json:"age of the universe"`
  age_of_the_earth : Array[AgeOfTheEarth] // `json:"age of the earth"`
  date : String // `json:"date"`
} derive(Show, Eq)

pub fn to_json(self : AutoGenerated) -> Json {
  {
    "age of the universe": self.age_of_the_universe.to_json(),
    "age of the earth": self.age_of_the_earth.to_json(),
    "date": self.date.to_json(),
  }
}

pub struct AgeOfTheUniverse {
  name : String // `json:"name"`
  value : Double // `json:"value"`
} derive(Show, Eq)

pub fn to_json(self : AgeOfTheUniverse) -> Json {
  {
    "name": self.name.to_json(),
    "value": self.value.to_json(),
  }
}

pub struct AgeOfTheEarth {
  name : String // `json:"name"`
  value : Int64 // `json:"value"`
} derive(Show, Eq)

pub fn to_json(self : AgeOfTheEarth) -> Json {
  {
    "name": self.name.to_json(),
    "value": self.value.to_json(),
  }
}
