pub struct AutoGenerated {
  booleanfield : Bool
  somearray : Array[Somearray]
  date : String
} derive(Show, Eq)

pub fn to_json(self : AutoGenerated) -> Json {
  {
    "booleanfield": self.booleanfield.to_json(),
    "somearray": self.somearray.to_json(),
    "date": self.date.to_json(),
  }
}

pub struct Somearray {
  id : Json
  name : String
  features : Json
} derive(Show, Eq)

pub fn to_json(self : Somearray) -> Json {
  {
    "id": self.id.to_json(),
    "name": self.name.to_json(),
    "features": self.features.to_json(),
  }
}
